VITE_BACKEND_URL=http://localhost:3002
# Sent only when VITE_BACKEND_URL is not localhost (production APIM).
VITE_MS_API_SUBSCRIPTION_KEY=your-apim-subscription-key
# Grafana Faro Frontend Observability (baked into Vite build at compile time)
VITE_GRAFANA_FARO_URL=https://your-faro-collector-url
